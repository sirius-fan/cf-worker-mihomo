{
  "name": "cf-worker-mihomo-server",
  "version": "1.0.0",
  "description": "Mihomo配置生成服务器版本",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node --watch server.js",
    "build": "node esbuild.js",
    "deploy": "vercel deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "esbuild": "^0.19.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}